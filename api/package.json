{
  "name": "drewclark-portfolio-api",
  "version": "1.0.0",
  "description": "Azure Functions for DrewClark.io portfolio",
  "scripts": {
    "start": "func start",
    "watch": "func start --watch",
    "test-openai": "node test-api.js",
    "diagnostic": "node api-diagnostic.js",
    "diag-cors": "node api-diagnostic.js --cors --verbose",
    "test": "echo \"No tests configured for API yet\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.8.4",
    "dotenv": "^16.4.7",
    "glob": "^11.0.1"
  }
}
